---
description: 'Large Outside logo for footers.'
labels: ['web', 'asset', 'svg', 'icon']
---

import { OutsideLearnLogo } from './outside-learn-logo';

### Default ProfileIcon

```js live
<OutsideLearnLogo />
```

### Custom color by using the fillColor prop

Modify the text to see it change live:

```js live
<OutsideLearnLogo fillColor="teal" />
```
