---
description: 'LockShadowIcon renders a yellow lock icon outlined in black with a red shadow'
labels: ['asset', 'svg', 'icon', 'lock', 'shadow']
---

import { LockShadowIcon } from './lock-shadow-icon';

## Import

```js
    import { LockShadowIcon } from './lock-shadow-icon';
    <LockShadowIcon />
```

## SVG Lock Shadow Icon component

A basic SVG that shows a padlock shape in Outside yellow (#EA2127) outlined in black with a red shadow (#FDD20A)

## Add classes to the wrapping SVG element

Modify props to see it change live:
```js live
    <LockShadowIcon classes="w-full" />
```